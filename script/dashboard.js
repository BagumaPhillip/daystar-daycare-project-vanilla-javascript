document.addEventListener('DOMContentLoaded', function () {
    const babyTable = document.getElementById('babyTable').querySelector('tbody');

    // Fetch babies data from local storage when the page loads
    renderBabiesFromLocalStorage();

    function renderBabiesFromLocalStorage() {
        // Fetch and render babies data from local storage
    }
});
